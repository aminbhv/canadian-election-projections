line-length = 120
target-version = "py310"

[lint]
select = ["E", "F", "I", "B", "UP"]

[lint.flake8-bugbear]
# NoiseModel is a frozen (immutable) dataclass, so it is safe as a default argument.
extend-immutable-calls = ["election.simulate.NoiseModel", "NoiseModel"]
